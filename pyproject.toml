[project]
name = "fourier-embedding-research"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "tiktoken>=0.13.0",
]

[dependency-groups]
dev = ["pytest>=8.0", "matplotlib>=3.7"]

[tool.pytest.ini_options]
testpaths = ["tests"]